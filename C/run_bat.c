#include <stdio.h>
#include <stdlib.h>

void main ()
{
    //float hz = 1;
    //scanf("%f",&hz);
    //printf(%f,hz);

    //system("nvidia-smi -i 0 --loop-ms=1000 --format=csv,noheader --query-gpu=power.draw");
    system("RUN.bat");
    //printf("1");
}
