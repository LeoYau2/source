coordinates=numpy.zeros((100,4))#一個二維整數陣列儲存辨識結果，coordinate[0,0]儲存一個整數代表有幾個辨識出的物體，coordinate[1]以後儲存方框的座標，[1,0]是左上x座標，[1,1]是左上y座標，[1,2]是右下x座標，[1,3]是右下y座標

#灰階圖片儲存在一個二維陣列裡

def knn(food,bit_depth):
    """
    這裡寫辨識的程式
    """
    #模擬輸出
    coordinates[0,0] = 3 #辨識出三個物體
    coordinates[1,:] = [12,52,63,87] #第一個方框的座標
    coordinates[2,:] = [75,86,103,120] #第二個方框的座標
    coordinates[3,:] = [52,97,79,130] #第三個方框的座標



#使用方式
knn("tomato",8) #(要辨識的物體,圖片的位元深度)
